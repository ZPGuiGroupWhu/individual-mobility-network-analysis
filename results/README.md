# Results Directory

This directory is intentionally empty in the clean code release.

Running `python scripts/run_all.py --dataset all` will create intermediate
outputs, logs, and manuscript-side tables under this directory.
